FROM n8nio/n8n:latest

# Install ffmpeg
USER root
RUN apk add --no-cache ffmpeg

# n8n basic auth (optional but recommended)
ENV N8N_BASIC_AUTH_ACTIVE=true
ENV N8N_BASIC_AUTH_USER=admin
ENV N8N_BASIC_AUTH_PASSWORD=strongpassword

# Render port
ENV N8N_PORT=10000
EXPOSE 10000

USER node
CMD ["n8n"]
